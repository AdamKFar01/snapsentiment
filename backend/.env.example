# No secrets needed for local dev — this is just a placeholder in case
# you later add an API key or model hub token.

# HuggingFace Hub token (optional — only needed to access gated models)
# HF_TOKEN=hf_...
